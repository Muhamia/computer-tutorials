### 2.4.4 三维数组和高维数组

我们之前已经介绍了一维数组和二维数组

相比再把维度增加，大家也应该能推导出来是什么样了

#### 三维数组的创建

创建一个存储3个二维数组，每个二维数组包含4个一维数组，每个一维数组可存储5个元素的三维数组:
```c
int arr3[3][4][5];
```

你想理解为长宽高也可以，长为5宽为4高为3，这样就是一个长方体



剩下的，至于赋值、遍历等操作，就和前面大同小异了

至于更高维的数组，大家思考推理一下，应该也不难知道该怎么做

而且，一般三维数组及更高维的数组就并不常用了

大家熟练掌握一维数组和二维数组即可