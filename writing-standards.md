# 行文规范

## 基础标准

先将知名的 [中文文案排版指北](https://github.com/sparanoid/chinese-copywriting-guidelines/blob/master/README.zh-Hans.md) 目录照搬过来。

> - 空格
>   - 中英文之间需要增加空格
>   - 中文与数字之间需要增加空格
>   - 数字与单位之间需要增加空格
>   - 全角标点与其他字符之间不加空格
> - 标点符号
>   - 不重复使用标点符号
> - 全角和半角
>   - 使用全角中文标点
>   - 数字使用半角字符
>   - 遇到完整的英文整句、特殊名词，其内容使用半角标点
> - 名词
>   - 专有名词使用正确的大小写
>   - 不要使用不地道的缩写
> - 争议
>   - 链接之间增加空格
>   - 简体中文使用直角引号

对于“争议”部分，这里的规定是：

- 链接与其他文本之间 **要** 增加空格。
- 简体中文 **不** 使用直角引号，而是使用弯引号。

## 扩展标准

### 换行

两个“块”之间必须有空行。以下是“块”的一些例子：

- 普通的段落（行）
- 表格
- 代码块
- 标题

因此，在编写普通文本时，换行也必须使用两个换行符。不要使用行尾空格。

除非是在编写表格、列表、代码块等东西，否则换行符不能单个出现。

例：

````markdown
# 测试代码

## 文本行

这是一行文本。

这是另一行文本。

## 代码块

下面有代码块。

```python3
print("Hello, world!")
```

## 无序列表

下面有无序列表。

- 这是一项
- 这是另一项
- 这是最后一项

## 表格

下面还有一个小表格。

| 这是一列 | 这是另一列 |
|:--------:|:----------:|
| 这是一项 | 这是另一项 |

## 图片

下面有一张图。

![](path/to/image.png)

上面有一张图。
````


### 行内代码

行内的任何程序代码必须使用反引号包裹。

例：

```markdown
模除运算所使用的运算符是 `%` 。

动态分配内存可以使用标准库提供的 `malloc` 函数。

C11 标准将一些以 `_s` 结尾的函数纳入标准库中，作为可选的扩展。
```

### 空格

Markdown 特有的记法要与其他文本之间有空格。例如，有序列表的 `1.` 和后面的文字之间，以及标题的 `#` 和后面的文字之间，都需要有空格。

例：

```markdown
这里有 **粗体** 字。

这里有 *斜体* 字。

- 无序列表的一项
- 无序列表的另一项

1. 有序列表的第一项
2. 有序列表的第二项

## 这是一个小标题

这里有一个 [链接](www.example.com) 。

C 语言标准库提供 `fopen` 函数，它能够打开文件。
```
